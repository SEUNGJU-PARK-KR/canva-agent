name: Temporary Largo relaxed May holdout

on:
  push:
    branches:
      - research/largo-relaxed-july-holdout-20260903
    paths:
      - ".github/workflows/tmp-largo-relaxed-may-holdout.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: largo-relaxed-may-holdout
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check requests beautifulsoup4

      - name: Reconstruct May candidates
        shell: bash
        run: |
          set -euo pipefail
          rm -rf may-holdout
          mkdir -p may-holdout/raw may-holdout/output may-holdout/source
          PYTHONPATH=scripts python research/largo-target3-20day/source/backfill_largo_target3_20day.py \
            --output-dir may-holdout/raw \
            --end-date 2026-05-29 \
            --days 22 \
            --preselect 80 \
            --workers 24 \
            2>&1 | tee may-holdout/output/backfill.log

      - name: Export every May candidate outcome
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL \
            https://raw.githubusercontent.com/SEUNGJU-PARK-KR/canva-agent/research/largo-v6-june-2026-backtest/research/largo-close-v6-shadow/export_june_candidate_outcomes.py \
            -o may-holdout/source/export_month_candidate_outcomes.py
          python may-holdout/source/export_month_candidate_outcomes.py \
            --detail may-holdout/raw/target3_20day_detail.json \
            --output may-holdout/output/may-candidate-outcomes.csv \
            --month 2026-05 \
            --workers 18 \
            2>&1 | tee may-holdout/output/outcome-export.log

      - name: Validate output
        run: |
          set -euo pipefail
          python - <<'PY'
          import csv, json
          from pathlib import Path
          root=Path('may-holdout/output')
          rows=list(csv.DictReader((root/'may-candidate-outcomes.csv').open(encoding='utf-8-sig')))
          summary=json.loads((root/'may-candidate-outcomes.summary.json').read_text(encoding='utf-8'))
          assert summary['month']=='2026-05'
          assert len(summary['dates']) >= 17
          assert len(rows) >= 400
          assert summary['known_outcomes'] >= len(rows)-3
          print({k:summary[k] for k in ('rows','dates','known_outcomes','baseline_hit3_rate','baseline_mean_policy3_pct')})
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: largo-relaxed-may-holdout
          path: may-holdout
          retention-days: 90
