name: LargoTV fast genuine playback check

on:
  push:
    branches:
      - largo-fast-playback-pilot-20260825
    paths:
      - ".github/workflows/largo-fast-playback.yml"
      - "scripts/largo_fast_playback.py"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/largo-fast-playback.yml"
      - "scripts/largo_fast_playback.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: largo-fast-playback-pilot-20260825
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Playwright
        run: |
          python -m pip install --upgrade pip playwright
          python -m py_compile scripts/largo_fast_playback.py
          google-chrome --version || chromium --version || true
      - name: Check real advancing playback
        run: python scripts/largo_fast_playback.py largo_fast_playback_output
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: largo-fast-playback
          path: largo_fast_playback_output
          if-no-files-found: warn
          retention-days: 7
          compression-level: 6
